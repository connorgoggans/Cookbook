% Pandoc-compatible preamble for commands that pandoc can handle natively.
% Included via pandoc -H macros.tex

% nicefrac support — any remaining \nicefrac commands not caught by preprocessing
\newcommand{\nicefrac}[2]{#1/#2}
